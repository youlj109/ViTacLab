"""Direct medium-difficulty dexterous tasks package.

Gym environments are registered inside each task subpackage (e.g. ``forge_dexhand/``,
``unscrewing_bottle_cap/``, ``bi_peg/``), so importing ``ViTacLab.tasks`` will pick them up automatically.
"""
